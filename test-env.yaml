name: index-gxa
channels:
  - conda-forge
  - ebi-gene-expression-group
  - bioconda
dependencies:
  - bash
  - curl
  - jq
  - pyyaml
  - pandas
  - atlas-web-bulk-cli

  # - bats-core # bats provided by atlas-web-bulk-cli
  # - openjdk=11 provided by atlas-web-bulk-cli
